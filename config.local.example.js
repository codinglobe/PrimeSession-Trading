// Kopiere diese Datei nach `config.local.js` und trage deine Werte ein.
// `config.local.js` ist in .gitignore und bleibt lokal.
window.PS_CONFIG = {
  SUPABASE_URL: 'https://<project-ref>.supabase.co',
  SUPABASE_ANON_KEY: '<dein-anon-key>',

  // Optional
  ADMIN_USERNAME: 'admin',
  ADMIN_EMAIL: 'codinglobe@gmail.com'
};
